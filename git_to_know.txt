#published branch on remote server -> start working on that topic too
git checkout --track origin/dev
#This creates a new local branch with the same name as the remote one-
#and directly establishes a tracking connection between the two
